spring:
  profiles:
    active: local

server:
  port: 8080

springdoc:
  packages-to-scan: com.ojicoin.cookiepang.controller
  paths-to-match: /**


cloud:
  aws:
    credentials:
      instanceProfile: false
    region:
      static: ap-northeast-2
    stack:
      auto: false
